spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/1SpringIsComing1/config-server
          clone-on-start: true
      fail-fast: true